#include <stdio.h>
#include "main.h"

/**
 * _islower - check upper or lowercase.
 * @c - takes input
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	if (a >= 97 && a <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

