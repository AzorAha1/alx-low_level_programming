#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * int_abs - print sign of number.
 * @abs: takes input
 *
 * Description - this is the description
 * Return: Always 0 (Success)
 */
int _abs(int abs)
{
	if (abs >= 0)
	{
		abs = abs;
	}
	else
	{
		abs = -abs;
	}

	return (0);
}

