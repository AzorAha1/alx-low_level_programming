#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * _abs - print sign of number.
 * @n: takes input
 *
 * Description - this is the description
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	int absolute;

	absolute = abs(n);
	return (absolute);
}

