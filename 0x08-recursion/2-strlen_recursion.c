#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - print string
 * @s: takes char
 * Description - function
 * Return: return type
 */
int _strnlen_recursion(char *s)
{
	int l;

	l = strlen(s);
	return(l);
}
