#include <stdio.h>
#include <string.h>
#include<math.h>
/**
 * _pow_recursion - pow
 * @x: takes int
 * @y: takes int
 * Description - function
 * Return: return type
 */
int _pow_recursion(int x, int y)
{
	int result;

	result = pow(x, y);
	return (result);
}
