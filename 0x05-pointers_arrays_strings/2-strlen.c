#include <stdio.h>
#include "main.h"
#include "string.h"
/**
 * swap_int - reset to 98
 * @a: takes int size
 * @b: takes int
 * Description - this is the description
 * Return: return value type
 */
int _strlen(char *s)
{
	int len = strlen(s);
	
	return (len);
}
