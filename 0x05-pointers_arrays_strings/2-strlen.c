#include <stdio.h>
#include "main.h"

/**
 * swap_int - reset to 98
 * @a: takes int size
 * @b: takes int
 * Description - this is the description
 * Return: return value type
 */
int _strlen(char *s)
{
	int i = strlen(*s);
	
	return (i);
}
