#include <stdio.h>
#include "main.h"
#include "string.h"

/**
 * _strlen - getting string length
 * @s: char s
 * Description - this is the description
 * Return: return value type
 */
int _strlen(char *s)
{
	int len = strlen(s);
	return (len);
}
